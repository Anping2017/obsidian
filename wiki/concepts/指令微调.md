---
title: 指令微调
type: concept
tags: [ai, mature]
sources: [raw/AI人工智能/04-前沿技术专题/04-01-大语言模型技术.md, raw/AI人工智能/03-应用层/03-02-自然语言处理/03-02-04-大语言模型原理.md]
created: 2026-05-05
updated: 2026-05-05
summary: 用指令-响应数据集对预训练大模型微调,使其学会理解并执行自然语言指令,是 ChatGPT 类对话模型的关键训练步骤。
---

# 指令微调

## 定义

**指令微调**(Instruction Tuning, Instruction Fine-Tuning)是用大量"**指令-响应**"对(instruction-response pairs)对预训练 [[大语言模型]] 进行有监督微调,使其学会**理解并执行各类自然语言指令**的训练技术。

它是从"会续写文本"的纯预训练模型,转化为"会回答指令"的对话/助手模型的关键步骤,InstructGPT(2022)与 Self-Instruct(2022)等论文奠定其方法学基础。

## 核心要点

### 在 LLM 训练流水线中的位置

```
预训练(Pre-training)                  → 续写大量文本,学习世界知识
        ↓
指令微调(Instruction Tuning, SFT)      → 学会执行指令
        ↓
偏好对齐(RLHF / DPO / Constitutional)  → 符合人类偏好
        ↓
专业能力增强(可选,代码、推理等专项)
```

预训练让模型有"知识",指令微调让模型有"能力",对齐让模型有"价值观"。

### 指令数据形式

#### 单轮

```
Instruction: 把以下英文翻译为中文:Hello, world.
Output: 你好,世界。
```

#### 带输入

```
Instruction: 总结这段文字。
Input: <长文本>
Output: <摘要>
```

#### 多轮对话

```
User: 写一个 Python 函数计算斐波那契数列
Assistant: <代码>
User: 改成迭代版本
Assistant: <修改后代码>
```

### 关键数据集

| 数据集 | 来源 | 特点 |
|---|---|---|
| **Natural Instructions**(2022) | 研究者收集 | 多任务、英语 |
| **FLAN / FLAN-T5**(Google) | 大量 NLP 任务转 instruction 形式 | 涵盖广 |
| **Alpaca**(Stanford) | GPT-3.5 自动生成 52K 条 | 引爆开源 instruction tuning |
| **Self-Instruct**(2022) | 模型自生成 instruction-response | 减少人工 |
| **OpenAssistant** | 社区众包 | 多语言 |
| **ShareGPT** | 用户分享的 ChatGPT 对话 | 多轮真实场景 |

### Self-Instruct 流水线

由 GPT-3 等强模型自动生成训练数据:
1. 写少量种子指令
2. 让模型扩充类似指令
3. 让模型生成响应
4. 自我过滤低质量样本
5. 用过滤后数据微调小模型

成本极低,Alpaca 仅花 600 美元就让 LLaMA 7B 达到接近 GPT-3.5 的指令遵循能力。

### 训练目标

标准的有监督学习,**最大化目标响应的对数似然**:

```
L = -Σ log P(response_t | instruction, response_<t)
```

只对响应部分计算损失,指令部分作为条件不参与梯度更新("masking the prompt")。

### 模板与控制 token

引入特殊符号区分角色:
```
<|system|>You are a helpful assistant.<|end|>
<|user|>How does photosynthesis work?<|end|>
<|assistant|>Photosynthesis is...<|end|>
```

不同模型有不同模板:Llama 用 `[INST]`、Claude 用对话格式、ChatML 用 `<|im_start|>`。

### 关键挑战

#### 1. 数据多样性

数据需覆盖:
- 任务类型(分类、生成、翻译、问答、推理)
- 难度梯度
- 领域分布(代码、数学、常识、创作)
- 语言

数据多样性对最终能力影响超过数据量。

#### 2. 数据质量

低质量响应会让模型学到错误模式。Alpaca 后期通过 GPT-4 重新生成,质量显著提升。

#### 3. 灾难性遗忘

指令微调可能损失部分预训练阶段获得的能力。
应对:
- 混入预训练数据
- 较小学习率、较少训练步数
- LoRA 等参数高效微调

#### 4. 与对齐的边界

指令微调让模型"更服从",但服从恶意指令也是问题。需配合后续的 RLHF / Constitutional AI 等对齐技术。

### 参数高效微调(PEFT)

全参数指令微调对硬件要求高,实用替代:
- **LoRA**:低秩适配器,仅训练少量参数
- **QLoRA**:量化 + LoRA,消费级 GPU 可微调 70B
- **P-tuning / Prefix Tuning**:仅训练 prompt 嵌入

### 与 In-Context Learning 的关系

[[少样本提示]] 在不修改模型权重的情况下用 in-context 示例引导。
- ICL 是推理时学习
- Instruction Tuning 是训练时学习
- 两者可叠加:Instruction-tuned 模型对 ICL 响应更好

### 评估方法

- **MT-Bench**:多轮对话评估
- **AlpacaEval**:用 GPT-4 作裁判对比
- **Vicuna 评估**:80 个开放性问题
- **Chatbot Arena**:人类盲评

### 行业演化

- 2022:InstructGPT、ChatGPT 引爆
- 2023:Alpaca、Vicuna、WizardLM 开源浪潮
- 2024:数据合成成为主流,质量超越数量
- 2025:多模态指令微调(图像 + 文本指令)

## 和其他概念的关系

- 是 [[预训练与微调]] 流水线的核心阶段
- 数据形式与 [[提示词工程]] 互为镜像
- 后接 [[RLHF]] / DPO / Constitutional AI 完成对齐
- 与 [[少样本提示]] 在能力获取路径上互补
- 用于训练 [[大语言模型]] 的 chat / instruct 变体
- 与 [[Anthropic]] / [[OpenAI]] 的产品级模型训练直接相关
- LoRA、QLoRA 是其参数高效化的关键工具

## 参考源

- raw/AI人工智能/04-前沿技术专题/04-01-大语言模型技术.md
- raw/AI人工智能/03-应用层/03-02-自然语言处理/03-02-04-大语言模型原理.md
