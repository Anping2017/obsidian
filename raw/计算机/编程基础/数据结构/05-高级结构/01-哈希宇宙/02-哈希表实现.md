# 哈希表实现

## 📖 核心概念

**哈希表（Hash Table）**是一种根据键（Key）直接访问内存存储位置的数据结构。它通过哈希函数将键映射到数组的索引，实现快速的数据访问。

### 🏗️ 哈希表特征

```mermaid
graph TD
    A[哈希表] --> B[哈希函数]
    A --> C[冲突处理]
    A --> D[负载因子]
    A --> E[性能特征]
    
    B --> B1[键到索引的映射]
    C --> C1[链地址法]
    C --> C2[开放地址法]
    D --> D1[存储元素数量/容量]
    E --> E1[O(1)平均时间复杂度]
```

## 🔧 哈希表实现

### 基础哈希表

```cpp
class HashTable {
private:
    struct Node {
        int key;
        int value;
        Node* next;
        
        Node(int k, int v) : key(k), value(v), next(nullptr) {}
    };
    
    vector<Node*> table;
    int capacity;
    int size;
    
    // 哈希函数
    int hashFunction(int key) {
        return key % capacity;
    }
    
    // 扩容
    void resize() {
        int oldCapacity = capacity;
        capacity *= 2;
        vector<Node*> newTable(capacity, nullptr);
        
        for (int i = 0; i < oldCapacity; ++i) {
            Node* current = table[i];
            while (current) {
                Node* next = current->next;
                int index = hashFunction(current->key);
                current->next = newTable[index];
                newTable[index] = current;
                current = next;
            }
        }
        
        table = newTable;
    }
    
public:
    HashTable(int cap = 16) : capacity(cap), size(0) {
        table.resize(capacity, nullptr);
    }
    
    // 插入
    void put(int key, int value) {
        int index = hashFunction(key);
        Node* current = table[index];
        
        // 检查是否已存在
        while (current) {
            if (current->key == key) {
                current->value = value;
                return;
            }
            current = current->next;
        }
        
        // 插入新节点
        Node* newNode = new Node(key, value);
        newNode->next = table[index];
        table[index] = newNode;
        size++;
        
        // 检查负载因子
        if (size > capacity * 0.75) {
            resize();
        }
    }
    
    // 获取
    int get(int key) {
        int index = hashFunction(key);
        Node* current = table[index];
        
        while (current) {
            if (current->key == key) {
                return current->value;
            }
            current = current->next;
        }
        
        return -1; // 未找到
    }
    
    // 删除
    bool remove(int key) {
        int index = hashFunction(key);
        Node* current = table[index];
        Node* prev = nullptr;
        
        while (current) {
            if (current->key == key) {
                if (prev) {
                    prev->next = current->next;
                } else {
                    table[index] = current->next;
                }
                delete current;
                size--;
                return true;
            }
            prev = current;
            current = current->next;
        }
        
        return false;
    }
    
    // 检查是否存在
    bool contains(int key) {
        return get(key) != -1;
    }
    
    // 获取大小
    int getSize() {
        return size;
    }
    
    // 是否为空
    bool isEmpty() {
        return size == 0;
    }
    
    // 清空
    void clear() {
        for (int i = 0; i < capacity; ++i) {
            Node* current = table[i];
            while (current) {
                Node* next = current->next;
                delete current;
                current = next;
            }
            table[i] = nullptr;
        }
        size = 0;
    }
    
    // 显示哈希表
    void display() {
        cout << "Hash Table:" << endl;
        cout << "===========" << endl;
        cout << "Capacity: " << capacity << endl;
        cout << "Size: " << size << endl;
        cout << "Load Factor: " << (double)size / capacity << endl;
        
        for (int i = 0; i < capacity; ++i) {
            if (table[i]) {
                cout << "Index " << i << ": ";
                Node* current = table[i];
                while (current) {
                    cout << "(" << current->key << ", " << current->value << ") ";
                    current = current->next;
                }
                cout << endl;
            }
        }
    }
    
    // 显示统计信息
    void displayStats() {
        cout << "Hash Table Statistics:" << endl;
        cout << "=====================" << endl;
        cout << "Capacity: " << capacity << endl;
        cout << "Size: " << size << endl;
        cout << "Load Factor: " << (double)size / capacity << endl;
        
        int emptySlots = 0;
        int maxChainLength = 0;
        int totalChains = 0;
        
        for (int i = 0; i < capacity; ++i) {
            if (table[i]) {
                totalChains++;
                int chainLength = 0;
                Node* current = table[i];
                while (current) {
                    chainLength++;
                    current = current->next;
                }
                maxChainLength = max(maxChainLength, chainLength);
            } else {
                emptySlots++;
            }
        }
        
        cout << "Empty Slots: " << emptySlots << endl;
        cout << "Non-empty Slots: " << totalChains << endl;
        cout << "Max Chain Length: " << maxChainLength << endl;
        cout << "Average Chain Length: " << (totalChains > 0 ? (double)size / totalChains : 0) << endl;
    }
};
```

### 开放地址法哈希表

```cpp
class OpenAddressingHashTable {
private:
    struct Entry {
        int key;
        int value;
        bool isDeleted;
        
        Entry() : key(-1), value(-1), isDeleted(false) {}
        Entry(int k, int v) : key(k), value(v), isDeleted(false) {}
    };
    
    vector<Entry> table;
    int capacity;
    int size;
    
    // 哈希函数
    int hashFunction(int key) {
        return key % capacity;
    }
    
    // 二次探测
    int quadraticProbe(int key, int i) {
        return (hashFunction(key) + i * i) % capacity;
    }
    
    // 扩容
    void resize() {
        int oldCapacity = capacity;
        capacity *= 2;
        vector<Entry> newTable(capacity);
        
        for (int i = 0; i < oldCapacity; ++i) {
            if (table[i].key != -1 && !table[i].isDeleted) {
                int key = table[i].key;
                int value = table[i].value;
                
                int index = hashFunction(key);
                int i = 0;
                while (newTable[index].key != -1) {
                    index = quadraticProbe(key, ++i);
                }
                newTable[index] = Entry(key, value);
            }
        }
        
        table = newTable;
    }
    
public:
    OpenAddressingHashTable(int cap = 16) : capacity(cap), size(0) {
        table.resize(capacity);
    }
    
    // 插入
    void put(int key, int value) {
        int index = hashFunction(key);
        int i = 0;
        
        // 查找插入位置
        while (table[index].key != -1 && table[index].key != key && !table[index].isDeleted) {
            index = quadraticProbe(key, ++i);
        }
        
        if (table[index].key == key) {
            table[index].value = value;
        } else {
            table[index] = Entry(key, value);
            size++;
            
            // 检查负载因子
            if (size > capacity * 0.5) {
                resize();
            }
        }
    }
    
    // 获取
    int get(int key) {
        int index = hashFunction(key);
        int i = 0;
        
        while (table[index].key != -1) {
            if (table[index].key == key && !table[index].isDeleted) {
                return table[index].value;
            }
            index = quadraticProbe(key, ++i);
        }
        
        return -1; // 未找到
    }
    
    // 删除
    bool remove(int key) {
        int index = hashFunction(key);
        int i = 0;
        
        while (table[index].key != -1) {
            if (table[index].key == key && !table[index].isDeleted) {
                table[index].isDeleted = true;
                size--;
                return true;
            }
            index = quadraticProbe(key, ++i);
        }
        
        return false;
    }
    
    // 检查是否存在
    bool contains(int key) {
        return get(key) != -1;
    }
    
    // 获取大小
    int getSize() {
        return size;
    }
    
    // 是否为空
    bool isEmpty() {
        return size == 0;
    }
    
    // 清空
    void clear() {
        for (int i = 0; i < capacity; ++i) {
            table[i] = Entry();
        }
        size = 0;
    }
    
    // 显示哈希表
    void display() {
        cout << "Open Addressing Hash Table:" << endl;
        cout << "==========================" << endl;
        cout << "Capacity: " << capacity << endl;
        cout << "Size: " << size << endl;
        cout << "Load Factor: " << (double)size / capacity << endl;
        
        for (int i = 0; i < capacity; ++i) {
            if (table[i].key != -1 && !table[i].isDeleted) {
                cout << "Index " << i << ": (" << table[i].key << ", " << table[i].value << ")" << endl;
            }
        }
    }
    
    // 显示统计信息
    void displayStats() {
        cout << "Open Addressing Hash Table Statistics:" << endl;
        cout << "=====================================" << endl;
        cout << "Capacity: " << capacity << endl;
        cout << "Size: " << size << endl;
        cout << "Load Factor: " << (double)size / capacity << endl;
        
        int emptySlots = 0;
        int deletedSlots = 0;
        
        for (int i = 0; i < capacity; ++i) {
            if (table[i].key == -1) {
                emptySlots++;
            } else if (table[i].isDeleted) {
                deletedSlots++;
            }
        }
        
        cout << "Empty Slots: " << emptySlots << endl;
        cout << "Deleted Slots: " << deletedSlots << endl;
        cout << "Occupied Slots: " << size << endl;
    }
};
```

### 字符串哈希表

```cpp
class StringHashTable {
private:
    struct StringNode {
        string key;
        int value;
        StringNode* next;
        
        StringNode(string k, int v) : key(k), value(v), next(nullptr) {}
    };
    
    vector<StringNode*> table;
    int capacity;
    int size;
    
    // 字符串哈希函数
    int hashFunction(string key) {
        int hash = 0;
        for (char c : key) {
            hash = (hash * 31 + c) % capacity;
        }
        return hash;
    }
    
    // 扩容
    void resize() {
        int oldCapacity = capacity;
        capacity *= 2;
        vector<StringNode*> newTable(capacity, nullptr);
        
        for (int i = 0; i < oldCapacity; ++i) {
            StringNode* current = table[i];
            while (current) {
                StringNode* next = current->next;
                int index = hashFunction(current->key);
                current->next = newTable[index];
                newTable[index] = current;
                current = next;
            }
        }
        
        table = newTable;
    }
    
public:
    StringHashTable(int cap = 16) : capacity(cap), size(0) {
        table.resize(capacity, nullptr);
    }
    
    // 插入
    void put(string key, int value) {
        int index = hashFunction(key);
        StringNode* current = table[index];
        
        // 检查是否已存在
        while (current) {
            if (current->key == key) {
                current->value = value;
                return;
            }
            current = current->next;
        }
        
        // 插入新节点
        StringNode* newNode = new StringNode(key, value);
        newNode->next = table[index];
        table[index] = newNode;
        size++;
        
        // 检查负载因子
        if (size > capacity * 0.75) {
            resize();
        }
    }
    
    // 获取
    int get(string key) {
        int index = hashFunction(key);
        StringNode* current = table[index];
        
        while (current) {
            if (current->key == key) {
                return current->value;
            }
            current = current->next;
        }
        
        return -1; // 未找到
    }
    
    // 删除
    bool remove(string key) {
        int index = hashFunction(key);
        StringNode* current = table[index];
        StringNode* prev = nullptr;
        
        while (current) {
            if (current->key == key) {
                if (prev) {
                    prev->next = current->next;
                } else {
                    table[index] = current->next;
                }
                delete current;
                size--;
                return true;
            }
            prev = current;
            current = current->next;
        }
        
        return false;
    }
    
    // 检查是否存在
    bool contains(string key) {
        return get(key) != -1;
    }
    
    // 获取大小
    int getSize() {
        return size;
    }
    
    // 是否为空
    bool isEmpty() {
        return size == 0;
    }
    
    // 清空
    void clear() {
        for (int i = 0; i < capacity; ++i) {
            StringNode* current = table[i];
            while (current) {
                StringNode* next = current->next;
                delete current;
                current = next;
            }
            table[i] = nullptr;
        }
        size = 0;
    }
    
    // 显示哈希表
    void display() {
        cout << "String Hash Table:" << endl;
        cout << "================" << endl;
        cout << "Capacity: " << capacity << endl;
        cout << "Size: " << size << endl;
        cout << "Load Factor: " << (double)size / capacity << endl;
        
        for (int i = 0; i < capacity; ++i) {
            if (table[i]) {
                cout << "Index " << i << ": ";
                StringNode* current = table[i];
                while (current) {
                    cout << "(" << current->key << ", " << current->value << ") ";
                    current = current->next;
                }
                cout << endl;
            }
        }
    }
    
    // 显示统计信息
    void displayStats() {
        cout << "String Hash Table Statistics:" << endl;
        cout << "============================" << endl;
        cout << "Capacity: " << capacity << endl;
        cout << "Size: " << size << endl;
        cout << "Load Factor: " << (double)size / capacity << endl;
        
        int emptySlots = 0;
        int maxChainLength = 0;
        int totalChains = 0;
        
        for (int i = 0; i < capacity; ++i) {
            if (table[i]) {
                totalChains++;
                int chainLength = 0;
                StringNode* current = table[i];
                while (current) {
                    chainLength++;
                    current = current->next;
                }
                maxChainLength = max(maxChainLength, chainLength);
            } else {
                emptySlots++;
            }
        }
        
        cout << "Empty Slots: " << emptySlots << endl;
        cout << "Non-empty Slots: " << totalChains << endl;
        cout << "Max Chain Length: " << maxChainLength << endl;
        cout << "Average Chain Length: " << (totalChains > 0 ? (double)size / totalChains : 0) << endl;
    }
};
```

## 🎯 哈希表应用

### 缓存系统

```cpp
class LRUCache {
private:
    struct Node {
        int key;
        int value;
        Node* prev;
        Node* next;
        
        Node(int k, int v) : key(k), value(v), prev(nullptr), next(nullptr) {}
    };
    
    unordered_map<int, Node*> cache;
    Node* head;
    Node* tail;
    int capacity;
    int size;
    
    // 添加节点到头部
    void addToHead(Node* node) {
        node->prev = head;
        node->next = head->next;
        head->next->prev = node;
        head->next = node;
    }
    
    // 删除节点
    void removeNode(Node* node) {
        node->prev->next = node->next;
        node->next->prev = node->prev;
    }
    
    // 移动节点到头部
    void moveToHead(Node* node) {
        removeNode(node);
        addToHead(node);
    }
    
    // 删除尾部节点
    Node* removeTail() {
        Node* lastNode = tail->prev;
        removeNode(lastNode);
        return lastNode;
    }
    
public:
    LRUCache(int cap) : capacity(cap), size(0) {
        head = new Node(0, 0);
        tail = new Node(0, 0);
        head->next = tail;
        tail->prev = head;
    }
    
    int get(int key) {
        if (cache.find(key) != cache.end()) {
            Node* node = cache[key];
            moveToHead(node);
            return node->value;
        }
        return -1;
    }
    
    void put(int key, int value) {
        if (cache.find(key) != cache.end()) {
            Node* node = cache[key];
            node->value = value;
            moveToHead(node);
        } else {
            Node* newNode = new Node(key, value);
            cache[key] = newNode;
            addToHead(newNode);
            size++;
            
            if (size > capacity) {
                Node* tail = removeTail();
                cache.erase(tail->key);
                delete tail;
                size--;
            }
        }
    }
    
    void display() {
        cout << "LRU Cache:" << endl;
        cout << "=========" << endl;
        cout << "Capacity: " << capacity << endl;
        cout << "Size: " << size << endl;
        
        Node* current = head->next;
        cout << "Cache order: ";
        while (current != tail) {
            cout << "(" << current->key << ", " << current->value << ") ";
            current = current->next;
        }
        cout << endl;
    }
};
```

### 频率统计

```cpp
class FrequencyCounter {
private:
    unordered_map<int, int> frequency;
    
public:
    void add(int value) {
        frequency[value]++;
    }
    
    int getFrequency(int value) {
        return frequency[value];
    }
    
    vector<pair<int, int>> getTopFrequent(int k) {
        vector<pair<int, int>> result;
        
        for (auto& pair : frequency) {
            result.push_back({pair.second, pair.first});
        }
        
        sort(result.rbegin(), result.rend());
        
        if (result.size() > k) {
            result.resize(k);
        }
        
        return result;
    }
    
    void display() {
        cout << "Frequency Counter:" << endl;
        cout << "================" << endl;
        
        for (auto& pair : frequency) {
            cout << "Value " << pair.first << ": " << pair.second << " times" << endl;
        }
    }
    
    void displayTopFrequent(int k) {
        cout << "Top " << k << " Frequent Values:" << endl;
        cout << "=============================" << endl;
        
        vector<pair<int, int>> topFrequent = getTopFrequent(k);
        
        for (auto& pair : topFrequent) {
            cout << "Value " << pair.second << ": " << pair.first << " times" << endl;
        }
    }
};
```

## 📊 哈希表分析

### 性能分析

```cpp
class HashTableAnalysis {
public:
    static void analyzePerformance() {
        cout << "Hash Table Performance Analysis:" << endl;
        cout << "===============================" << endl;
        
        cout << "1. Time Complexity:" << endl;
        cout << "   - Insert: O(1) average, O(n) worst" << endl;
        cout << "   - Search: O(1) average, O(n) worst" << endl;
        cout << "   - Delete: O(1) average, O(n) worst" << endl;
        
        cout << "2. Space Complexity:" << endl;
        cout << "   - O(n) for n elements" << endl;
        cout << "   - Additional space for collision handling" << endl;
        
        cout << "3. Hash Function Quality:" << endl;
        cout << "   - Uniform distribution" << endl;
        cout << "   - Minimal collisions" << endl;
        cout << "   - Fast computation" << endl;
        
        cout << "4. Collision Handling:" << endl;
        cout << "   - Chaining: O(1 + α) where α is load factor" << endl;
        cout << "   - Open Addressing: O(1/(1-α))" << endl;
    }
    
    static void analyzeSpaceComplexity() {
        cout << "Hash Table Space Complexity Analysis:" << endl;
        cout << "====================================" << endl;
        
        cout << "1. Chaining:" << endl;
        cout << "   - Array: O(m) where m is capacity" << endl;
        cout << "   - Linked lists: O(n) where n is size" << endl;
        cout << "   - Total: O(m + n)" << endl;
        
        cout << "2. Open Addressing:" << endl;
        cout << "   - Array: O(m) where m is capacity" << endl;
        cout << "   - Total: O(m)" << endl;
        
        cout << "3. Load Factor:" << endl;
        cout << "   - Optimal: 0.5 to 0.75" << endl;
        cout << "   - Too high: More collisions" << endl;
        cout << "   - Too low: Wasted space" << endl;
    }
    
    static void selectImplementation(bool needsOrdering, bool hasMemoryConstraints) {
        cout << "Implementation Selection:" << endl;
        cout << "========================" << endl;
        
        cout << "Needs ordering: " << (needsOrdering ? "Yes" : "No") << endl;
        cout << "Has memory constraints: " << (hasMemoryConstraints ? "Yes" : "No") << endl;
        
        cout << "Recommendation:" << endl;
        
        if (needsOrdering) {
            cout << "Use Ordered Hash Table or TreeMap" << endl;
        } else if (hasMemoryConstraints) {
            cout << "Use Open Addressing Hash Table" << endl;
        } else {
            cout << "Use Chaining Hash Table" << endl;
        }
    }
};
```

## 🎮 哈希表测试

### 1. 基础功能测试

```cpp
class HashTableTest {
public:
    static void testBasicHashTable() {
        cout << "Testing Basic Hash Table:" << endl;
        cout << "========================" << endl;
        
        HashTable hashTable(8);
        
        hashTable.put(1, 10);
        hashTable.put(2, 20);
        hashTable.put(3, 30);
        hashTable.put(4, 40);
        hashTable.put(5, 50);
        
        hashTable.display();
        hashTable.displayStats();
        
        cout << "Get key 3: " << hashTable.get(3) << endl;
        cout << "Contains key 2: " << hashTable.contains(2) << endl;
        
        hashTable.remove(2);
        cout << "After removing key 2:" << endl;
        hashTable.display();
    }
    
    static void testOpenAddressingHashTable() {
        cout << "Testing Open Addressing Hash Table:" << endl;
        cout << "=================================" << endl;
        
        OpenAddressingHashTable hashTable(8);
        
        hashTable.put(1, 10);
        hashTable.put(2, 20);
        hashTable.put(3, 30);
        hashTable.put(4, 40);
        hashTable.put(5, 50);
        
        hashTable.display();
        hashTable.displayStats();
        
        cout << "Get key 3: " << hashTable.get(3) << endl;
        cout << "Contains key 2: " << hashTable.contains(2) << endl;
        
        hashTable.remove(2);
        cout << "After removing key 2:" << endl;
        hashTable.display();
    }
    
    static void testStringHashTable() {
        cout << "Testing String Hash Table:" << endl;
        cout << "========================" << endl;
        
        StringHashTable hashTable(8);
        
        hashTable.put("apple", 1);
        hashTable.put("banana", 2);
        hashTable.put("cherry", 3);
        hashTable.put("date", 4);
        hashTable.put("elderberry", 5);
        
        hashTable.display();
        hashTable.displayStats();
        
        cout << "Get 'banana': " << hashTable.get("banana") << endl;
        cout << "Contains 'cherry': " << hashTable.contains("cherry") << endl;
        
        hashTable.remove("banana");
        cout << "After removing 'banana':" << endl;
        hashTable.display();
    }
    
    static void testLRUCache() {
        cout << "Testing LRU Cache:" << endl;
        cout << "================" << endl;
        
        LRUCache cache(3);
        
        cache.put(1, 10);
        cache.put(2, 20);
        cache.put(3, 30);
        cache.display();
        
        cout << "Get key 1: " << cache.get(1) << endl;
        cache.display();
        
        cache.put(4, 40);
        cache.display();
    }
    
    static void testFrequencyCounter() {
        cout << "Testing Frequency Counter:" << endl;
        cout << "========================" << endl;
        
        FrequencyCounter counter;
        
        vector<int> values = {1, 2, 3, 1, 2, 1, 3, 4, 2, 1};
        for (int value : values) {
            counter.add(value);
        }
        
        counter.display();
        counter.displayTopFrequent(3);
    }
    
    static void testAnalysis() {
        cout << "Testing Analysis:" << endl;
        cout << "===============" << endl;
        
        HashTableAnalysis::analyzePerformance();
        HashTableAnalysis::analyzeSpaceComplexity();
        HashTableAnalysis::selectImplementation(false, false);
    }
};
```

## 🔗 相关链接

- [[01-哈希宇宙|哈希宇宙]]
- [[02-哈希函数设计|哈希函数设计]]
- [[03-冲突处理策略|冲突处理策略]]

## 💡 哈希表要点

1. **哈希函数**: 将键映射到数组索引
2. **冲突处理**: 链地址法或开放地址法
3. **负载因子**: 影响性能的重要因素
4. **时间复杂度**: O(1)平均，O(n)最坏

---

*📝 哈希表提示：哈希表是实现快速查找的重要数据结构，掌握其实现原理有助于优化算法性能*
