# 进阶练习

## 练习概述

通过进阶练习提升CSS技能，掌握复杂布局和高级特性。

## 练习1：Grid布局系统

### 题目
使用CSS Grid创建一个复杂的响应式布局系统。

### 要求
1. 创建12列网格系统
2. 实现响应式断点
3. 包含嵌套网格
4. 使用网格区域命名

### 关键代码
```css
.grid-system {
    display: grid;
    grid-template-columns: repeat(12, 1fr);
    gap: 1rem;
    max-width: 1200px;
    margin: 0 auto;
}

.col-1 { grid-column: span 1; }
.col-2 { grid-column: span 2; }
.col-3 { grid-column: span 3; }
.col-4 { grid-column: span 4; }
.col-6 { grid-column: span 6; }
.col-12 { grid-column: span 12; }

@media (max-width: 768px) {
    .col-1, .col-2, .col-3, .col-4, .col-6 {
        grid-column: span 12;
    }
}
```

## 练习2：CSS变量系统

### 题目
构建一个完整的CSS变量系统，支持主题切换。

### 要求
1. 定义颜色、字体、间距变量
2. 实现明暗主题切换
3. 支持动态变量更新
4. 创建变量文档

### 关键代码
```css
:root {
    --primary-color: #007bff;
    --secondary-color: #6c757d;
    --font-size-base: 1rem;
    --spacing-unit: 0.25rem;
}

[data-theme="dark"] {
    --primary-color: #0d6efd;
    --secondary-color: #6c757d;
    --bg-color: #212529;
    --text-color: #ffffff;
}
```

## 练习3：复杂动画系统

### 题目
创建一个包含多种动画效果的交互系统。

### 要求
1. 实现页面过渡动画
2. 创建加载状态动画
3. 添加微交互效果
4. 优化动画性能

### 关键代码
```css
@keyframes pageTransition {
    0% { opacity: 0; transform: translateY(20px); }
    100% { opacity: 1; transform: translateY(0); }
}

.page-enter {
    animation: pageTransition 0.6s ease-out;
}

.loading-spinner {
    animation: spin 1s linear infinite;
}

@keyframes spin {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}
```

## 练习4：组件库架构

### 题目
设计一个可扩展的CSS组件库架构。

### 要求
1. 使用BEM命名规范
2. 实现组件变体系统
3. 支持主题定制
4. 提供使用文档

### 关键代码
```css
/* 基础组件 */
.btn {
    display: inline-block;
    padding: 0.75rem 1.5rem;
    border: none;
    border-radius: 0.25rem;
    cursor: pointer;
    transition: all 0.3s ease;
}

/* 组件变体 */
.btn--primary {
    background-color: var(--color-primary);
    color: var(--color-on-primary);
}

.btn--secondary {
    background-color: var(--color-secondary);
    color: var(--color-on-secondary);
}

/* 组件状态 */
.btn--disabled {
    opacity: 0.6;
    cursor: not-allowed;
}
```

## 练习5：性能优化

### 题目
优化CSS性能，实现最佳实践。

### 要求
1. 减少重排重绘
2. 优化选择器性能
3. 实现关键CSS
4. 使用现代CSS特性

### 关键代码
```css
/* 使用transform避免重排 */
.optimized-element {
    transform: translateX(100px);
    will-change: transform;
}

/* 高效选择器 */
.efficient-selector {
    /* 避免过度嵌套 */
}

/* 关键CSS内联 */
.critical-styles {
    /* 首屏关键样式 */
}
```

## 相关链接

- [[现代布局/Grid布局]] - 学习Grid布局
- [[CSS变量与自定义属性]] - 掌握CSS变量
- [[动画与过渡/关键帧动画]] - 了解复杂动画
- [[CSS架构/BEM方法论]] - 学习组件架构
- [[性能优化/渲染优化]] - 优化CSS性能

---

*下一步：学习 [[综合项目]] 完成完整项目*
