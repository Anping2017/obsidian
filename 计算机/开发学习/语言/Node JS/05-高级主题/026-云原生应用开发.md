# 云原生应用开发

```mermaid
graph TD
**云原生技术栈：**

| 技术 | 层级 | 作用 | 工具 |
|------|------|------|------|
| **容器化** | 应用层 | 环境一致性 | Docker |
| **编排** | 基础层 | 自动化管理 | Kubernetes |
| **服务网格** | 网络层 | 流量管理 | Istio |
| **CI/CD** | 流程层 | 持续交付 | GitHub Actions |

### 🔍 理解层：机制（Why&How）

**云原生架构原则：**

| 原则 | 描述 | 实践方法 |
|------|------|----------|
| **容器化** | 应用打包 | Docker镜像 |
| **微服务** | 服务拆分 | 独立部署 |
| **可观测性** | 监控追踪 | 日志+指标+链路 |
| **声明式** | 期望状态 | YAML配置 |

### 🚀 应用层：实践（Apply）

**Dockerfile最佳实践：**

```dockerfile
# ✅ 多阶段构建
FROM node:18-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production

COPY . .
RUN npm run build

FROM node:18-alpine AS production
WORKDIR /app
RUN addgroup -g 1001 -S nodejs
RUN adduser -S nodejs -u 1001

COPY --from=builder --chown=nodejs:nodejs /app/dist ./dist
COPY --from=builder --chown=nodejs:nodejs /app/node_modules ./node_modules
COPY --chown=nodejs:nodejs package*.json ./

USER nodejs
EXPOSE 3000
CMD ["node", "dist/index.js"]
```

**Kubernetes部署配置：**

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp-api
spec:
  replicas: 3
  selector:
    matchLabels:
      app: myapp-api
  template:
    metadata:
      labels:
        app: myapp-api
    spec:
      containers:
      - name: api
        image: myapp/api:v1.0.0
        ports:
        - containerPort: 3000
        env:
        - name: NODE_ENV
          value: "production"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
```

## 🧠 费曼学习法：能用简单的话解释

**云原生核心思想：**
1. **容器化** = 标准化的集装箱，能装任何货物
2. **编排** = 港口管理系统，自动调度船只
3. **自动化** = 无人港口，全自动运作

## 🎯 刻意练习要点

**必须掌握的技能：**
- [ ] 容器化应用打包
- [ ] Kubernetes集群管理
- [ ] 服务网格配置
- [ ] 自动化CI/CD

---

*🔗 相关链接：[[022-部署与运维策略]] | [[035-微服务项目实战]] | [[023-性能优化深入]]*
