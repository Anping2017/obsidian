# HTML快速参考手册

## 🎯 常用HTML标签速查

### 📋 结构标签

| 标签 | 用途 | 重要属性 |
|------|------|----------|
| `html` | 根标签 | `lang="zh-CN"` |
| `head` | head区域 | - |
| `body` | body内容 | - |
| `!DOCTYPE` | HTML5声明 | `html` |

### 🏷️ 语义化标签

| 标签 | 类型 | 用途 |
|------|------|------|
| `header` | 块级 | 头部区域 |
| `footer` | 块级 | 底部区域 |
| `nav` | 块级 | 导航菜单 |
| `main` | 块级 | 主要内容 |
| `section` | 块级 | 章节区域 |
| `article` | 块级 | 文章内容 |

### 📝 文本标签

| 标签 | 类型 | 用途 |
|------|------|------|
| `h1-h6` | 块级 | hi标题 |
| `p` | 块级 | 段落 |
| `strong` | 行内 | 重要强调 |
| `em` | 行内 | 一般强调 |
| `span` | 行内 | 文本容器 |

### 🔗 链接标签

| 标签 | 属性 | 用途 |
|------|------|------|
| `a` | `href="URL"` | 链接 |
| `img` | `src`, `alt` | 图片 |
| `video` | `controls` | 视频 |
| `audio` | `controls` | 音频 |

### 📋 表单标签

| 标签 | 属性 | 用途 |
|------|------|------|
| `form` | `method`, `action` | 表单 |
| `input` | `type`, `name` | 输入控件 |
| `select` | `name` | 下拉选择 |
| `textarea` | `rows`, `cols` | 文本域 |

## 🎨 CSS快速配对

### 🎯 常用CSS属性

```css
/* 盒模型 */
width: 300px;
height: 200px;
margin: 1rem;
padding: 2rem;
border: 1px solid #333;

/* 文本样式 */
font-size: 16px;
font-weight: bold;
font-style: italic;
line-height: 1.6;
text-align: center;

/* 布局 */
display: block;
display: inline;
display: flex;
display: grid;

/* 定位 */
position: relative;
position: absolute;
position: fixed;
top: 50px;
left: 100px;
```

## 🔧 JavaScript常用方法

### 📊 DOM操作

```javascript
// 获取元素
document.getElementById('myElement');
document.querySelector('.myClass');

// 修改内容
element.textContent = '新文本';
element.innerHTML = '<strong>新HTML</strong>';

// 样式操作
element.style.color = 'red';
element.classList.add('highlight');

// 事件监听
element.addEventListener('click', function() {
    console.log('点击事件');
});
```

## 📱 响应式设计断点

### 🎯 常用断点

```css
/* 移动设备 */
@media (max-width: 768px) {
    /* 移动端样式 */
}

/* 平板设备 */
@media (min-width: 769px) and (max-width: 1024px) {
    /* 平板样式 */
}

/* 桌面设备 */
@media (min-width: 1025px) {
    /* 桌面样式 */
}
```

## 🛠️ 开发工具

### 📊 验证工具

| 工具 | URL | 用途 |
|------|-----|------|
| **W3C Validator** | validator.w3.org | HTML验证 |
| **CSS Validator** | jigsaw.w3.org/css-validator | CSS验证 |
| **Markup Validator** | validator.w3.org/markup | 标记验证 |

### 🎯 性能工具

| 工具 | 用途 | 指标 |
|------|------|------|
| **Lighthouse** | 页面性能分析 | Core Web Vitals |
| **PageSpeed Insights** | Google性能测试 | 加载速度评分 |
| **GTmetrix** | 性能监控 | 详细性能报告 |

---

**💡 使用提示**：此参考手册收录了HTML开发和性能优化的核心要点，建议收藏并定期查阅。
